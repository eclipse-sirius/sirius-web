{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "format": {
      "outputs": []
    },
    "format-lint": {
      "outputs": [],
      "outputLogs": "errors-only"
    },
    "build": {
      "dependsOn": ["format-lint", "^build"],
      "env": ["SIRIUS_PLAYWRIGHT_COVERAGE"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "coverage": {
      "dependsOn": ["build"]
    },
    "publish:local": {
      "dependsOn": ["build"]
    },
    "build-dev": {
      "dependsOn": ["format-lint", "^build-dev"],
      "cache": false,
      "outputLogs": "hash-only"
    },
    "start": {
      "dependsOn": ["format-lint", "^build-dev"],
      "persistent": true,
      "cache": false
    }
  }
}
