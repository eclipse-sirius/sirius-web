= Studio maker guide

This studio maker guide explains how to define the authoring models (domains, views, automation) that power {product}.
Whenever we mention the *“view”* or *“view based representation descriptions”*, we refer to the representation descriptions that are used to configure how representations behave inside {product}.
