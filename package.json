{
  "name": "sirius-components",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd packages/app && yarn start",
    "build": "cd packages/app && yarn build",
    "test": "cd packages/app && yarn test",
    "coverage": "cd packages/app && yarn coverage",
    "lint": "lerna run --parallel lint",
    "format": "lerna run --parallel format",
    "format-lint": "lerna run --parallel --loglevel silent format-lint"
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "react-scripts": "^2.0.0-next.3e165448"
  },
  "workspaces": [
    "packages/*"
  ]
}
